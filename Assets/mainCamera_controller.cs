using UnityEngine;
using System.Collections;

public class mainCamera_controller : MonoBehaviour {

	/*GameObject mario;
	Vector3 relCamPos = Vector3.zero;

	// Use this for initialization
	void Start () {
		mario = GameObject.FindGameObjectWithTag ("Player");

		relCamPos = transform.position - mario.transform.position;
	}
	
	// Update is called once per frame
	void Update () {
		Vector3 newCamCoords = Vector3.zero;
		newCamCoords = mainCamera.transform.position;
		newCamCoords.x = mario.transform.position.x;
		mainCamera.transform.position = newCamCoords;
	}*/
}
